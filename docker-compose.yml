services:
  secure-auth:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATA_DIR=/data
      - CRON_DIR=/cron
      - TZ=UTC
    volumes:
      - seed-data:/data
      - cron-output:/cron
    restart: unless-stopped

volumes:
  seed-data:
  cron-output:
